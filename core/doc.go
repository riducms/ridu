// Package core owns Ridu's application contracts and runtime.
//
// Most applications should import the root ridu package, which re-exports the
// stable authoring surface. Extensions that need the underlying application
// contract may import core directly, following the same launcher/core split as
// PocketBase.
package core
