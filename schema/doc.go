// Package schema defines the immutable, versioned representation of a resolved
// Ridu configuration.
//
// Resolved schemas contain declarative metadata only. They never contain
// executable access rules, secrets, or database connections.
package schema
