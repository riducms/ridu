// Package store defines public document-storage and transaction contracts.
// Adapters consume the shared query AST and must apply access predicates
// atomically with the requested operation.
package store
