# `@riducms/protocol`

Versioned TypeScript wire types shared by generated clients, the Fetch SDK, and the Ridu admin.

Use the version installed by your generated project so its protocol types match the SDK and server.
