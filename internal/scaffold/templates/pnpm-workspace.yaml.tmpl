packages:
  - "admin"
  - ".ridu/packages/*"

# Release rehearsals provision unpublished Ridu packages in the workspace.
# Published packages still resolve normally when no matching local package exists.
linkWorkspacePackages: true
